parent=keycloak.v2
import=common/keycloak

# The Caputchin reference login theme. It inherits everything from keycloak.v2 and only overrides
# the login, reset-credentials and registration page templates to render the verification widget
# from the attributes the extension exposes. Select it as the realm Login theme, or set
# parent=caputchin on your own theme to inherit the widget rendering.
