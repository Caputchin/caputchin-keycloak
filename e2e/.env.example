# Copy to e2e/.env and fill in a local dev site key + secret.
# Mint one in the local Caputchin dashboard (http://localhost:3000) and allowlist
# http://localhost:8081 (this Keycloak origin) on the key.

CPT_PUB=cpt_pub_xxxxxxxxxxxxxxxx
CPT_SEC=cpt_sec_xxxxxxxxxxxxxxxx
